require 'spec_helper'

describe SigfoxClient do
  it 'has a version number' do
    expect(SigfoxClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
